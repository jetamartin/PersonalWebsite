<%= form_for [@blog_post, @comment || @blog_post.comments.build], html: { class: "form-horizontal" } do |f| %>
<% if @blog_post.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@blog_post.errors.count, "error") %> prohibited this Comment from being saved:</h2>

    <ul>
    <% @blog_post.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
<% end %>
  <div class="form-group">
    <%= f.hidden_field :parent_id %>
    <%= f.label :name, class: "control-label col-sm-2 " %>
    <div class="col-sm-10">
      <%= f.text_field :name, class: "form-control", placeholder: "Enter your name here" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :message, class: " control-label col-sm-2" %>
    <div class="col-sm-10">
      <%= f.text_area :message, class: "form-control", placeholder: "Enter your message here" %>
    </div>
  </div>
  <div class="form-group actions">
    <%=f.submit %>
  </div>

<% end %>
